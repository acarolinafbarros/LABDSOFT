
@model IEnumerable<GAM.Models.PMA.ValidaMatchViewModel>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div class="panel panel-default">
    <!-- Default panel contents -->
    <div class="panel-heading">Lista de Casais</div>

    <!-- Table -->
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.MatchStatsId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Centro)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Data)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CasalId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Dador)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.AmostraId)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MatchStatsId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Centro)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Data)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CasalId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Dador)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AmostraId)
                </td>
                <td>
                    <a asp-action="ValidaMatchAmostra" asp-route-id="@item.PedidoGametasId"><button type="button" class="btn btn-default"><span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span></button></a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>

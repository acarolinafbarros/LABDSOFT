@model IEnumerable<GAM.Models.Questionarios.Questionario>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Area)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Area)
            </td>
            <td>
                <a asp-action="Preview" asp-route-id="@item.QuestionarioId">Preview</a> |
                <a asp-action="Edit" asp-route-id="@item.QuestionarioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QuestionarioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QuestionarioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
